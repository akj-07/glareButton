# Node.js dependencies
node_modules/

# Build output (e.g., TypeScript build output, distribution files)
dist/
build/

# Logs
npm-debug.log
yarn-debug.log
yarn-error.log

# Environment variables
.env

# TypeScript files (e.g., cache files)
*.tsbuildinfo

# OS-specific files
.DS_Store        # macOS
Thumbs.db         # Windows

# IDE and editor files
.vscode/          # VS Code settings
.idea/            # JetBrains IDEs (IntelliJ, WebStorm, etc.)

# Coverage reports (for testing tools like Jest)
coverage/

# Storybook files (optional, if you want to exclude them)
.storybook/

# Optional: Ignore the 'glarebutton' file being used as a dependency locally (if you're using the file path for npm install)
glarebutton/

# Optional: If you are using testing tools like Jest or Mocha, ignore related files
*.test.js
*.test.ts
*.test.tsx
*.snap
